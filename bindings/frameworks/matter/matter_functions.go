// Code generated by go-bindings-purecg. DO NOT EDIT.
//go:build darwin

package matter

import (
	"github.com/ebitengine/purego/objc"

	"github.com/deploymenttheory/go-bindings-macosplatform/bindings/frameworks/foundation"
)

var (
	// Resolve Matter attribute IDs into a descriptive string. For unknown IDs, a string '<Unknown clusterID %d>' (if the cluster ID is not known) or '<Unknown attributeID %d>' (if the cluster ID is known but the attribute ID is not known) will be returned.
	_fnMTRAttributeNameForID func(MTRClusterIDType, MTRAttributeIDType) objc.ID
	// Resolve Matter cluster IDs into a descriptive string. For unknown IDs, a string '<Unknown clusterID %d>' will be returned.
	_fnMTRClusterNameForID               func(MTRClusterIDType) objc.ID
	_fnMTRDeviceControllerStorageClasses func() *foundation.NSSet[objc.Class]
	// Resolve Matter event IDs into a descriptive string. For unknown IDs, a string '<Unknown clusterID %d>' (if the cluster ID is not known) or '<Unknown eventID %d>' (if the cluster ID is known but the event ID is not known) will be returned.
	_fnMTREventNameForID func(MTRClusterIDType, MTREventIDType) objc.ID
	// Resolve Matter request (client to server) command IDs into a descriptive string. For unknown IDs, a string '<Unknown clusterID %d>' (if the cluster ID is not known) or '<Unknown commandID %d>' (if the cluster ID is known but the command ID is not known) will be returned.
	_fnMTRRequestCommandNameForID func(MTRClusterIDType, MTRCommandIDType) objc.ID
	// Resolve Matter response (server to client) command IDs into a descriptive string. For unknown IDs, a string '<Unknown clusterID %d>' (if the cluster ID is not known) or '<Unknown commandID %d>' (if the cluster ID is known but the command ID is not known) will be returned.
	_fnMTRResponseCommandNameForID func(MTRClusterIDType, MTRCommandIDType) objc.ID
	// Arranges for log messages from the Matter stack to be delivered to a callback block. @param logTypeThreshold only messages up to (and including) the specified log type will be delivered @param callback the block to call, or nil to disable the log callback. The callback block may be called concurrently and/or from arbitrary threads. It SHALL NOT call back directly or indirectly into any Matter APIs, nor block the calling thread for a non-trivial amount of time.
	_fnMTRSetLogCallback func(MTRLogType, objc.Block)
	// Set the Message Reliability Protocol parameters for all controllers, including already-running ones.  This allows control over retransmit delays to account for high-latency networks. Since MRP parameters are communicated to peers during session setup, existing sessions will not be affected when this function is called, but all sessions established after the call will be. Setting all arguments to nil will reset to the MRP parameters to their default values. Setting some arguments to non-nil will change just those values, keeping current values for any arguments that are nil (not resetting them to defaults). Non-nil arguments are specified as an integer number of milliseconds. @param idleRetransmitMs the minimal interval between retransmits for someone sending messages to us, when they think we are "idle" and might have our radio only turned on intermittently. @param activeRetransmitMs the minimal interval between retransmits for someone sending messages to us, when they think we are "active" and have the radio turned on consistently. @param activeThresholdMs the amount of time we will stay in "active" mode after network activity. @param additionalRetransmitDelayMs additional delay between retransmits for messages we send, on top of whatever delay the other side requests via its MRP parameters.
	_fnMTRSetMessageReliabilityParameters func(objc.ID, objc.ID, objc.ID, objc.ID)
)

// Resolve Matter attribute IDs into a descriptive string. For unknown IDs, a string '<Unknown clusterID %d>' (if the cluster ID is not known) or '<Unknown attributeID %d>' (if the cluster ID is known but the attribute ID is not known) will be returned.
func MTRAttributeNameForID(clusterID MTRClusterIDType, attributeID MTRAttributeIDType) *foundation.NSString {
	_ret := _fnMTRAttributeNameForID(clusterID, attributeID)
	if _ret != 0 {
		_ret.Send(objc.RegisterName("retain"))
	}
	return foundation.NSStringFromID(_ret)
}

// Resolve Matter cluster IDs into a descriptive string. For unknown IDs, a string '<Unknown clusterID %d>' will be returned.
func MTRClusterNameForID(clusterID MTRClusterIDType) *foundation.NSString {
	_ret := _fnMTRClusterNameForID(clusterID)
	if _ret != 0 {
		_ret.Send(objc.RegisterName("retain"))
	}
	return foundation.NSStringFromID(_ret)
}

func MTRDeviceControllerStorageClasses() *foundation.NSSet[objc.Class] {
	return _fnMTRDeviceControllerStorageClasses()
}

// Resolve Matter event IDs into a descriptive string. For unknown IDs, a string '<Unknown clusterID %d>' (if the cluster ID is not known) or '<Unknown eventID %d>' (if the cluster ID is known but the event ID is not known) will be returned.
func MTREventNameForID(clusterID MTRClusterIDType, eventID MTREventIDType) *foundation.NSString {
	_ret := _fnMTREventNameForID(clusterID, eventID)
	if _ret != 0 {
		_ret.Send(objc.RegisterName("retain"))
	}
	return foundation.NSStringFromID(_ret)
}

// Resolve Matter request (client to server) command IDs into a descriptive string. For unknown IDs, a string '<Unknown clusterID %d>' (if the cluster ID is not known) or '<Unknown commandID %d>' (if the cluster ID is known but the command ID is not known) will be returned.
func MTRRequestCommandNameForID(clusterID MTRClusterIDType, commandID MTRCommandIDType) *foundation.NSString {
	_ret := _fnMTRRequestCommandNameForID(clusterID, commandID)
	if _ret != 0 {
		_ret.Send(objc.RegisterName("retain"))
	}
	return foundation.NSStringFromID(_ret)
}

// Resolve Matter response (server to client) command IDs into a descriptive string. For unknown IDs, a string '<Unknown clusterID %d>' (if the cluster ID is not known) or '<Unknown commandID %d>' (if the cluster ID is known but the command ID is not known) will be returned.
func MTRResponseCommandNameForID(clusterID MTRClusterIDType, commandID MTRCommandIDType) *foundation.NSString {
	_ret := _fnMTRResponseCommandNameForID(clusterID, commandID)
	if _ret != 0 {
		_ret.Send(objc.RegisterName("retain"))
	}
	return foundation.NSStringFromID(_ret)
}

// Arranges for log messages from the Matter stack to be delivered to a callback block. @param logTypeThreshold only messages up to (and including) the specified log type will be delivered @param callback the block to call, or nil to disable the log callback. The callback block may be called concurrently and/or from arbitrary threads. It SHALL NOT call back directly or indirectly into any Matter APIs, nor block the calling thread for a non-trivial amount of time.
func MTRSetLogCallback(logTypeThreshold MTRLogType, callback func(MTRLogType, *foundation.NSString, *foundation.NSString)) {
	var __block_callback objc.Block
	if callback != nil {
		__block_callback = objc.NewBlock(func(_ objc.Block, blockParam0 MTRLogType, blockParam1 objc.ID, blockParam2 objc.ID) {
			if blockParam1 != 0 {
				blockParam1.Send(objc.RegisterName("retain"))
			}
			if blockParam2 != 0 {
				blockParam2.Send(objc.RegisterName("retain"))
			}
			callback(blockParam0, foundation.NSStringFromID(blockParam1), foundation.NSStringFromID(blockParam2))
		})
		defer __block_callback.Release()
	}
	_fnMTRSetLogCallback(logTypeThreshold, __block_callback)
}

// Set the Message Reliability Protocol parameters for all controllers, including already-running ones.  This allows control over retransmit delays to account for high-latency networks. Since MRP parameters are communicated to peers during session setup, existing sessions will not be affected when this function is called, but all sessions established after the call will be. Setting all arguments to nil will reset to the MRP parameters to their default values. Setting some arguments to non-nil will change just those values, keeping current values for any arguments that are nil (not resetting them to defaults). Non-nil arguments are specified as an integer number of milliseconds. @param idleRetransmitMs the minimal interval between retransmits for someone sending messages to us, when they think we are "idle" and might have our radio only turned on intermittently. @param activeRetransmitMs the minimal interval between retransmits for someone sending messages to us, when they think we are "active" and have the radio turned on consistently. @param activeThresholdMs the amount of time we will stay in "active" mode after network activity. @param additionalRetransmitDelayMs additional delay between retransmits for messages we send, on top of whatever delay the other side requests via its MRP parameters.
func MTRSetMessageReliabilityParameters(idleRetransmitMs *foundation.NSNumber, activeRetransmitMs *foundation.NSNumber, activeThresholdMs *foundation.NSNumber, additionalRetransmitDelayMs *foundation.NSNumber) {
	_fnMTRSetMessageReliabilityParameters(idleRetransmitMs.Ptr(), activeRetransmitMs.Ptr(), activeThresholdMs.Ptr(), additionalRetransmitDelayMs.Ptr())
}
