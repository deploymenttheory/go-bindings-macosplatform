// Code generated by go-bindings-purecg. DO NOT EDIT.
//go:build darwin

package compositorservices

type MTLViewport struct {
	OriginX float64
	OriginY float64
	Width   float64
	Height  float64
	Znear   float64
	Zfar    float64
}

// An opaque type that contains the textures and other information you need to set up your render pipeline. Use the drawable type to retrieve the textures for your render pipelines, and use the drawable’s views to get details about how to render to those textures. Get the drawable for a frame using the “cp_frame_query_drawable“ function. The layer manages a limited number of reusable drawable types and recycles them after each use. Draw only one frame at a time to ensure each new frame’s drawable type is ready in time.
// C struct: cp_drawable
// CpDrawable is an opaque type.
type CpDrawable struct{}

// An opaque type that contains the drawable types and other information you need to set up your render pipeline. See “cp_drawable_t“ for more information about drawables.
// C struct: cp_drawable_array
// CpDrawableArray is an opaque type.
type CpDrawableArray struct{}

// An object that can render any effects associated with a drawable This is required for applications that want to render using the Progressive Immersion Style
// C struct: cp_drawable_render_context
// CpDrawableRenderContext is an opaque type.
type CpDrawableRenderContext struct{}

// An opaque type that provides access to the timing information and data types you need to render a single frame of content. During each cycle of your app’s rendering loop, retrieve a frame using the “cp_layer_renderer_query_next_frame“ function and use it to render your app’s content. The “cp_frame_t“ type provides access to the information you need to render your frame in a timely manner. It also provides access to the “cp_drawable_t“ type you use to retrieve the textures you need for drawing. Separate the work you do for each frame into two phases: the update phase and the encode phase. Use the update phase to perform app-specific tasks, such as processing interactions with your content. Use the encode phase to commit the Metal commands you need to draw your content for the current pose. For both phases, use frame functions to notify the Compositor when you start and stop the related work. For example, surround update work with calls to the “cp_frame_start_update“ and “cp_frame_end_update“ functions.
// C struct: cp_frame
// CpFrame is an opaque type.
type CpFrame struct{}

// An opaque type that stores information about a frame’s encoding, rendering, and presentation deadlines. Before you start drawing, call the “cp_frame_predict_timing“ function to update and retrieve the frame’s timing information. After you access the frame’s “cp_drawable_t“ type, get the timing information using the “cp_drawable_get_frame_timing“ function.
// C struct: cp_frame_timing
// CpFrameTiming is an opaque type.
type CpFrameTiming struct{}

// An opaque type that describes a hover effect of the tracking area.
// C struct: cp_hover_effect
// CpHoverEffect is an opaque type.
type CpHoverEffect struct{}

// An opaque type that specifies the configuration of one of the layer’s drawable textures. For direct-to-screen drawing, you specify how you want Compositor to configure the required textures when you configure your layer. For a device with multiple displays, Compositor can configure the textures in different ways. For example, it can create a separate texture for each display, or it can combine the images from multiple displays into a single texture. A texture topology type contains the details of one of the textures Compositor creates for your app.
// C struct: cp_texture_topology
// CpTextureTopology is an opaque type.
type CpTextureTopology struct{}

// A Mach absolute time clock value.
// C struct: cp_time
type CpTime struct {
	Cp_mach_abs_time uint64
}

// An opaque type that describes a region of a view that interacts with the gaze/cursor.
// C struct: cp_tracking_area
// CpTrackingArea is an opaque type.
type CpTrackingArea struct{}

// An opaque type that manages the information for how to render content into the current frame. Compositor provides a view for each distinct render viewpoint. For example, a head-mounted display typically contains two views: one for each eye. Use the information in the views to set up your render pass descriptor. For example, use it to determine which part of a texture to fill with content.
// C struct: cp_view
// CpView is an opaque type.
type CpView struct{}

// An opaque type that describes the relationship between a view and the underlying texture. A texture map helps you locate the content for a specific view within a texture. Texture maps are especially important when a layer uses a single texture to manage multiple views. For example, a head-mounted display might store the images for both the left and right eyes in a single texture. Pass this type to other functions to get specific details about the current texture, such as its view bounds or its index into a texture array.
// C struct: cp_view_texture_map
// CpViewTextureMap is an opaque type.
type CpViewTextureMap struct{}

// CpTimeT is an alias for cp_time (C typedef cp_time_t).
type CpTimeT = CpTime
