{{/*
    file.tmpl — the generated-file scaffold: the DO-NOT-EDIT header, the build
    constraint, the package clause, the (already-sorted) import block, and the
    rendered body. gofmt (via the caller's go/format pass) canonicalises the
    result, so only the tokens and their order matter here, not the spacing.
*/}}
{{define "file"}}{{.Header}}
{{if .BuildTag}}
{{.BuildTag}}
{{end}}
package {{.PkgName}}

{{if .ImportLines}}import (
{{range .ImportLines}}{{.}}
{{end}})

{{end}}{{.Body}}{{end}}
