{{/*
    mock_interface.tmpl — the exported <Type>able interface (Unwrap + every
    generated method signature) plus a compile-time implementation assertion, for
    mocking/DI. Method signature Lines are pre-rendered in Go so they match the
    method writers exactly.
*/}}
{{define "mock_interface"}}// {{.IfaceName}} is the interface implemented by [{{.TypeName}}], for mocking and DI.
type {{.IfaceName}} interface {
Unwrap() *{{.RawType}}
{{range .Lines}}{{.}}
{{end}}}

var _ {{.IfaceName}} = (*{{.TypeName}})(nil)

{{end}}
