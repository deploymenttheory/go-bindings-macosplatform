{{/*
    plain_method.tmpl renders one Go method (or package-level function) that calls
    a single Objective-C method and converts the arguments and result between Go
    and Objective-C. The whole body is assembled in Go and passed in as Body, so
    this template only lays out the doc comment, signature, and body.
*/}}
{{define "plain_method"}}{{if .DocComment}}{{.DocComment}}{{end}}func {{.Recv}}{{.GoName}}({{.ParamStr}}){{.RetSig}} {
{{.Body}}
}

{{end}}
