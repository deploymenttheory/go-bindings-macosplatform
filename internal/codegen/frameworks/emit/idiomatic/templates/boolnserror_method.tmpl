{{/*
    boolnserror_method.tmpl — a (BOOL, NSError**) raw method surfaced as a method
    returning only error. Recv is "(x *Type) " for an instance method or "" for a
    package-level class function; Target is the receiver/raw-package expression.
*/}}
{{define "boolnserror_method"}}// {{.GoName}} returns any validation error.
func {{.Recv}}{{.GoName}}() error {
_, err := {{.Target}}.{{.RawGoName}}()
return err
}

{{end}}
